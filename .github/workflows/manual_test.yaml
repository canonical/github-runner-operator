name: Manual test

on:
  # push:
  pull_request:
  workflow_dispatch:

jobs:
  amd64:
    runs-on: [self-hosted, medium, amd64]
    steps:
      - run: lscpu
      - run: df -h
      - run: free -h
      - run: uname -a
      - run: lsb_release -a
      - uses: actions/checkout@v5
  s390x:
    runs-on: [self-hosted, s390x]
    steps:
      - run: lscpu
      - run: df -h
      - run: free -h
      - run: uname -a
      - run: lsb_release -a
      - uses: actions/checkout@v5
