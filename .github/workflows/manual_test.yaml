name: Manual test

on:
  # push:
  pull_request:
  workflow_dispatch:

jobs:
  edge:
    runs-on: [self-hosted, edge]
    steps:
      - run: lscpu
      - run: df -h
      - run: free -h
      - run: uname -a
      - run: lsb_release -a
      - name: Setup tmate session
        uses: canonical/action-tmate@main 