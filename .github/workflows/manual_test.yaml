name: Manual test

on:
  # push:
  pull_request:
  workflow_dispatch:

jobs:
  noble-large-amd64:
    strategy:
      fail-fast: false
      matrix:
        index: [0]
    runs-on: [self-hosted, reactive-amd64-noble-large-ps7]
    steps:
      - run: lscpu
      - run: df -h
      - run: free -h
      - run: uname -a
      - run: lsb_release -a
      - name: Setup tmate session
        uses: canonical/action-tmate@main