name: Manual test

on:
  # push:
  pull_request:
  workflow_dispatch:

jobs:
  edge-tmate:
    runs-on: [self-hosted, staging,amd64,self-hosted-linux-amd64-jammy-staging,stg-reactive]
    steps:
      - run: lscpu
      - run: df -h
      - run: free -h
      - run: uname -a
      - run: lsb_release -a
      - uses: actions/checkout@v4