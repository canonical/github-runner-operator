name: Manual test

on:
  # push:
  pull_request:
  workflow_dispatch:

jobs:
  jammy-test:
    runs-on: [self-hosted, jammy, large]
    steps:
      - run: lscpu
      - run: df -h
      - run: free -h
      - run: uname -a
      - run: lsb_release -a
      - name: tmate ssh
        uses: canonical/action-tmate@main