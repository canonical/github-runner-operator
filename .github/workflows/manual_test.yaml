name: Manual test

on:
  # push:
  pull_request:
  workflow_dispatch:

jobs:
  edge:
    runs-on: [self-hosted, edge, amd64, reactive]
    steps:
      - run: lscpu
      - run: df -h
      - run: free -h
      - run: uname -a
      - run: lsb_release -a
      - run: sleep 100000