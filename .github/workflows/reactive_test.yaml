name: Reactive Test
on: push
jobs:
  build:
    runs-on: ["self-hosted", "self-hosted-linux-amd64-jammy-staging", "ps7"]
    # runs-on: ["self-hosted", "stg-reactive"]
    # runs-on: ["self-hosted", "stg-private-endpoint"]
    # runs-on: ["self-hosted"]
    timeout-minutes: 30
    steps:
        - name: sleep for 6 minutes
          run: |
            echo "starting running"
            ip a
            exit 1
            sleep 10
