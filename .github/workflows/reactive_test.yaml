name: Reactive Test
on: push
jobs:
  build:
    # runs-on: ["self-hosted", "stg-reactive"]
    runs-on: ["self-hosted", "ps7"]
    steps:
        - name: sleep for 6 minutes
          run: |
            eccho "starting running"
            exit 1
            sleep 360
        - name: Setup tmate session
          if: ${{ failure() }}
          uses: canonical/action-tmate@main
