name: Tests

on:
  pull_request:
  #workflow_call:

jobs:
  unit-tests:
    uses: canonical/operator-workflows/.github/workflows/test.yaml@self-hosted-runner
    with:
      self-hosted-runner: true
    secrets: inherit
